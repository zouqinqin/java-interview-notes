### java 内存模型
- 【java内存模型】时Java Memory Model (JMM)的意思

- 简单的理解，JMM定义了一套在多线程读写共享数据时，（成员变量，数组）时，对数据的可见性，有序性和原子性的规则和保障

- 原子性
- synchronized (同步关键字)
```java
synchronized(对象){
    要作为原子操作代码
}
```
- 用synchronized 解决并发问题