## 04-01

### 今日计划
1. 学习JVM堆内存,堆内存的诊断
2. 方法区定义和作用
3. StringTable常见面试题,字符串的拼接，与串池的关系，优化
4. 直接内存的基本使用，内存溢出，释放原理等

### 明日计划
- 垃圾回收
- 强弱引用 

## 04-02 

### 今日计划
1. 学习JVM堆和方法区,堆是垃圾回收的主要区域，方法区 JVM1.8的实现是MateSpace元空间,独立于堆空间，存在系统内存中，由于系统内存较大，不容易发生内存溢出
2. StringTable 串池 和直接内存的学习

## 04-03

### 今日计划
1. 总结过去一年做过的项目
2. 梳理财务知识

## 4-07 周二
1. 学习垃圾回收器的使用和调优
2. G1的深入分析
3. 工作：差异调整预提uat的的问题修复

## 4-08 周三
- AccrualAdjustProcessTask 定时任务分析
1. 查询待处理的批次 
 ```sql
        SELECT t.batch_id
        FROM cloud_accrual_adjust_title t
        WHERE t.approve_status = '审批通过'
        AND NOT EXISTS (
            SELECT 1 FROM cloud_accrual_adjust_result r
            WHERE r.batch_id = t.batch_id
        )
        ORDER BY t.approve_time ASC
        LIMIT #{limit}
```
2. 查询明细数据  SELECT * FROM cloud_accrual_adjust_detail where barch_id in ()
3. 插入结果表  insert into cloud_accrual_adjust_result
4. 垃圾回收器案例分析和 visaulvm 工具的使用

### 面试时怎么说这段经验
- 可以这样描述：

- "我们项目 web 层和 service 层之间用 Thrift 做 RPC 通信，先定义 .thrift 文件描述接口和数据结构，编译器生成 Java 代码后，service 层实现 Iface 接口，web 层通过 Client 发起调用。
- 相比 HTTP+JSON，Thrift 二进制协议在内网传输性能更好，而且接口契约通过 .thrift 文件强约束，不容易出现字段不一致的问题。"